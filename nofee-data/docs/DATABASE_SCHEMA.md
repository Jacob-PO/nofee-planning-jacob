# 노피 데이터베이스 스키마 문서

**작성일**: 2025-11-19
**데이터베이스**: db_nofee
**목적**: 노피 서비스의 DB 구조 상세 문서

---

## 📋 목차

1. [개요](#개요)
2. [핵심 테이블](#핵심-테이블)
3. [사용자 관련 테이블](#사용자-관련-테이블)
4. [신청 관련 테이블](#신청-관련-테이블)
5. [상품 관련 테이블](#상품-관련-테이블)
6. [판매점 관련 테이블](#판매점-관련-테이블)
7. [리뷰 관련 테이블](#리뷰-관련-테이블)
8. [코드 테이블](#코드-테이블)

---

## 개요

노피 서비스는 총 **62개의 테이블**로 구성되어 있으며, 크게 다음과 같이 분류됩니다:

- **사용자**: 고객 정보 관리
- **신청**: 견적 신청 및 개통 프로세스
- **상품**: 휴대폰, 요금제, 가격표
- **판매점**: 매장 및 파트너 관리
- **리뷰**: 고객 리뷰 시스템
- **관리**: 어드민 및 에이전시 계정

### 주요 데이터 현황 (2025-11-19 기준)

| 카테고리 | 주요 테이블 | 데이터 수 |
|---------|-----------|----------|
| 사용자 | `tb_user` | 5,429명 |
| 일반 견적 | `tb_apply_phone` | 4,101건 |
| 캠페인 견적 | `tb_apply_campaign_phone` | 4,772건 |
| 매장 구매 | `tb_store_purchase` | 3,206건 |
| 판매점 | `tb_store` | 56개 |
| 리뷰 | `tb_review_*` (전체) | 1,701건 |
| 상품 | `tb_product_group_phone` | 151개 |
| 가격표 | `tb_pricetable_store_phone_row` | 779개 |

---

## 핵심 테이블

### 1. 사용자 (`tb_user`)

**목적**: 노피 서비스 가입 고객 정보

| 컬럼명 | 타입 | 설명 | 비고 |
|--------|------|------|------|
| `user_no` | int(11) | 사용자 번호 (PK) | Auto Increment |
| `user_id` | varchar(100) | 사용자 ID (이메일) | 암호화 |
| `user_pw` | varchar(255) | 비밀번호 | 해시 |
| `user_nm` | blob | 사용자 이름 | 암호화 |
| `user_tel_no` | blob | 전화번호 | 암호화 |
| `user_birth_dt` | date | 생년월일 | - |
| `user_gender` | varchar(1) | 성별 (M/F) | - |
| `marketing_yn` | varchar(1) | 마케팅 수신 동의 (Y/N) | - |
| `created_at` | timestamp | 생성일시 | - |
| `modified_at` | timestamp | 수정일시 | - |
| `deleted_yn` | varchar(1) | 삭제 여부 (Y/N) | Soft Delete |
| `deleted_at` | timestamp | 삭제일시 | - |

**데이터**: 5,429명
**특징**: 개인정보는 AES 암호화 저장

---

### 2. 일반 견적 신청 (`tb_apply_phone`)

**목적**: 버전 1 일반견적 신청 데이터 (노피 중개 방식)

| 컬럼명 | 타입 | 설명 | 비고 |
|--------|------|------|------|
| `apply_no` | int(11) | 신청 번호 (PK) | Auto Increment |
| `user_no` | int(11) | 사용자 번호 (FK) | → tb_user |
| `user_nm` | blob | 신청자 이름 | 암호화 |
| `apply_tel_no` | blob | 신청자 전화번호 | 암호화 |
| `apply_product_group_code` | varchar(100) | 신청 상품 그룹 코드 | - |
| `apply_product_code` | varchar(100) | 신청 상품 코드 | - |
| `apply_storage_code` | varchar(10) | 저장 용량 코드 | - |
| `apply_color_no` | int(11) | 색상 번호 | - |
| `apply_carrier_code` | varchar(10) | 통신사 코드 | SKT/KT/LG |
| `apply_rate_plan_code` | varchar(100) | 요금제 코드 | - |
| `apply_join_type_code` | varchar(10) | 가입 유형 코드 | 신규/번호이동/기기변경 |
| `apply_installment_principal` | int(11) | 할부원금 (노피 제시 가격) | - |
| `apply_month_price` | int(11) | 월 납부액 | - |
| `store_no` | int(11) | 매칭된 판매점 번호 (FK) | → tb_store |
| **`step_code`** | varchar(10) | **진행 상태 코드** | **중요!** |
| `completed_at` | timestamp | 개통 완료 일시 | step_code='0201005' |
| `created_at` | timestamp | 신청 일시 | - |
| `deleted_yn` | varchar(1) | 삭제 여부 (Y/N) | Soft Delete |

**데이터**: 4,101건 (2025-07 ~ 2025-11)
**특징**:
- 노피가 전국 평균 가격 제시 (`apply_installment_principal`)
- 지역 기반 자동 매장 매칭 (`store_no`)
- 개통율: 2.8%

#### Step Code 값

| Code | 상태 | 설명 |
|------|------|------|
| 0201001 | 신청접수 | 고객이 신청 완료 |
| 0201002 | 진행중 | 매장 DB 구매 |
| 0201003 | 대응완료 | 매장 선택 완료 |
| 0201004 | 개통 진행중 | 방문 예약 완료 |
| 0201005 | 개통완료 | 최종 개통 완료 ✅ |
| 0201006 | 반려 | 매장 반려 또는 고객 취소 |
| 0201007 | 취소 | 매칭 실패 |

---

### 3. 캠페인 견적 신청 (`tb_apply_campaign_phone`)

**목적**: 버전 2/3 동네성지 신청 데이터 (판매점 직접 업로드 방식)

| 컬럼명 | 타입 | 설명 | 비고 |
|--------|------|------|------|
| `apply_no` | int(11) | 신청 번호 (PK) | Auto Increment |
| `user_no` | int(11) | 사용자 번호 (FK) | → tb_user |
| `user_nm` | blob | 신청자 이름 | 암호화 |
| `apply_tel_no` | blob | 신청자 전화번호 | 암호화 |
| `apply_product_group_code` | varchar(100) | 신청 상품 그룹 코드 | 고객이 선택한 상품 |
| `apply_product_code` | varchar(100) | 신청 상품 코드 | - |
| `apply_carrier_code` | varchar(10) | 통신사 코드 | - |
| `apply_join_type_code` | varchar(10) | 가입 유형 코드 | - |
| `apply_installment_principal` | int(11) | 할부원금 (판매점 제시 가격) | - |
| `apply_discount_price` | int(11) | 할인 금액 | - |
| `apply_discount_rate` | int(11) | 할인율 (%) | - |
| `store_no` | int(11) | 판매점 번호 (FK) | 고객이 선택한 판매점 |
| **`step_code`** | varchar(10) | **진행 상태 코드** | tb_apply_phone과 동일 |
| `completed_at` | timestamp | 개통 완료 일시 | - |
| `created_at` | timestamp | 신청 일시 | - |
| `deleted_yn` | varchar(1) | 삭제 여부 (Y/N) | - |

**데이터**: 4,772건 (2025-07 ~ 현재)
**특징**:
- 고객이 판매점 상품 직접 선택 (`store_no`, `apply_product_group_code`)
- 판매점이 가격 직접 설정 (`apply_installment_principal`)
- 버전 2 (2025-07~08): 개통율 0.5%
- 버전 3 (2025-09~현재): 개통율 6.0% (최고 15.3%)

---

### 4. 매장 구매 확정 (`tb_store_purchase`)

**목적**: 판매점이 고객 DB를 구매 확정한 내역

| 컬럼명 | 타입 | 설명 | 비고 |
|--------|------|------|------|
| `purchase_no` | int(11) | 구매 번호 (PK) | Auto Increment |
| `apply_no` | int(11) | 신청 번호 (FK) | → tb_apply_phone or tb_apply_campaign_phone |
| `store_no` | int(11) | 판매점 번호 (FK) | → tb_store |
| `purchase_at` | timestamp | 구매 확정 일시 | - |
| `created_at` | timestamp | 생성 일시 | - |
| `deleted_yn` | varchar(1) | 삭제 여부 (Y/N) | - |

**데이터**: 3,206건
**특징**:
- 신청 → 매장 구매 전환율: 36.13%
- 매장 구매 → 개통 전환율: 7.49%
- 판매점의 적극성 지표

---

### 5. 판매점 (`tb_store`)

**목적**: 노피 파트너 판매점 정보

| 컬럼명 | 타입 | 설명 | 비고 |
|--------|------|------|------|
| `store_no` | int(11) | 판매점 번호 (PK) | Auto Increment |
| `store_nm` | blob | 판매점명 | 암호화 |
| `nickname` | varchar(100) | 닉네임 (공개용) | - |
| `sido_no` | int(11) | 시도 번호 (FK) | → tb_area_sido |
| `sigungu_no` | int(11) | 시군구 번호 (FK) | → tb_area_sigungu |
| `business_no` | blob | 사업자 등록번호 | 암호화 |
| `tel_no` | blob | 대표 전화번호 | 암호화 |
| `pricetable_exposure_yn` | varchar(1) | 가격표 노출 여부 (Y/N) | 동네성지 활성화 |
| `step_code` | varchar(10) | 승인 상태 코드 | 0202003: 승인완료 |
| `created_at` | timestamp | 등록 일시 | - |
| `deleted_yn` | varchar(1) | 삭제 여부 (Y/N) | - |

**데이터**: 56개 (활성 11개)
**특징**:
- `pricetable_exposure_yn='Y'` AND `step_code='0202003'`: 동네성지 활성 판매점
- 지역별 분포: 서울, 경기 중심

---

### 6. 상품 그룹 (`tb_product_group_phone`)

**목적**: 휴대폰 기종 정보

| 컬럼명 | 타입 | 설명 | 비고 |
|--------|------|------|------|
| `product_group_code` | varchar(100) | 상품 그룹 코드 (PK) | - |
| `product_group_nm` | varchar(255) | 상품 그룹명 | 예: 갤럭시 S25 울트라 |
| `manufacturer_code` | varchar(10) | 제조사 코드 | Samsung, Apple 등 |
| `release_dt` | date | 출시일 | - |
| `state_code` | varchar(10) | 상태 코드 | 0204002: 활성 |
| `thumb_img_location` | varchar(255) | 썸네일 이미지 경로 | S3 |
| `thumb_img_nm` | varchar(255) | 썸네일 이미지 파일명 | - |
| `created_at` | timestamp | 생성일시 | - |
| `deleted_yn` | varchar(1) | 삭제 여부 (Y/N) | - |

**데이터**: 151개 (활성 75개)
**특징**:
- `state_code='0204002'`: 현재 판매 가능 상품
- 실제 가격표 연결된 상품: 26개 기종

---

### 7. 가격표 (`tb_pricetable_store_phone_row`)

**목적**: 판매점별 휴대폰 실제 판매 가격

| 컬럼명 | 타입 | 설명 | 비고 |
|--------|------|------|------|
| `price_no` | int(11) | 가격표 번호 (PK) | Auto Increment |
| `store_no` | int(11) | 판매점 번호 (FK) | → tb_store |
| `product_code` | varchar(100) | 상품 코드 (FK) | → tb_product_phone |
| `product_group_code` | varchar(100) | 상품 그룹 코드 (FK) | → tb_product_group_phone |
| `carrier_code` | varchar(10) | 통신사 코드 | SKT/KT/LG |
| `join_type_code` | varchar(10) | 가입 유형 코드 | 신규/번호이동/기기변경 |
| `installment_principal` | int(11) | **할부원금 (실제 판매가)** | 음수: 공짜폰 |
| `pricetable_dt` | date | 가격표 적용일 | - |
| `created_at` | timestamp | 생성일시 | - |
| `deleted_yn` | varchar(1) | 삭제 여부 (Y/N) | - |

**데이터**: 779개 (활성)
**특징**:
- 판매점이 직접 등록/관리
- `installment_principal` 음수: 공짜폰 (통신사 지원금 > 기기값)
- 동네성지 핵심 데이터

---

### 8. 리뷰 시스템

노피는 다양한 리뷰 테이블을 운영:

#### `tb_review_store_phone` (판매점 리뷰)
- **데이터**: 1,701건
- **특징**: 개통 완료 후 고객이 판매점 평가

| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| `review_no` | int(11) | 리뷰 번호 (PK) |
| `apply_no` | int(11) | 신청 번호 (FK) |
| `store_no` | int(11) | 판매점 번호 (FK) |
| `rating` | int(1) | 별점 (1~5) |
| `content` | text | 리뷰 내용 |
| `real_yn` | varchar(1) | 실제 리뷰 여부 (Y/N) |

**통계**:
- 실제 리뷰: 16건
- 가상 리뷰: 1,685건 (초기 신뢰도 확보용)

---

## 신청 관련 테이블

### 신청 메모 (`tb_apply_phone_memo`, `tb_apply_campaign_phone_memo`)

**목적**: 판매점이 신청 건에 남기는 메모

| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| `memo_no` | int(11) | 메모 번호 (PK) |
| `apply_no` | int(11) | 신청 번호 (FK) |
| `store_no` | int(11) | 판매점 번호 (FK) |
| `memo_content` | text | 메모 내용 |
| `created_at` | timestamp | 작성 일시 |

**데이터**:
- `tb_apply_phone_memo`: 3,838건
- `tb_apply_campaign_phone_memo`: 482건

### 개통 회원 정보 (`tb_apply_phone_user`, `tb_apply_campaign_phone_user`)

**목적**: 개통을 위한 고객 상세 정보 (신분증 등)

| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| `user_info_no` | int(11) | 정보 번호 (PK) |
| `apply_no` | int(11) | 신청 번호 (FK) |
| `user_no` | int(11) | 사용자 번호 (FK) |
| `id_file_location` | varchar(255) | 신분증 파일 경로 (S3) |
| `id_file_nm` | varchar(255) | 신분증 파일명 |

**데이터**:
- `tb_apply_phone_user`: 475건
- `tb_apply_campaign_phone_user`: 104건

---

## 상품 관련 테이블

### 상품 상세 (`tb_product_phone`)

**목적**: 휴대폰 상세 정보 (용량, 색상 등)

| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| `product_code` | varchar(100) | 상품 코드 (PK) |
| `product_group_code` | varchar(100) | 상품 그룹 코드 (FK) |
| `storage_code` | varchar(10) | 저장 용량 코드 |
| `product_nm` | varchar(255) | 상품명 |
| `retail_price` | int(11) | 출고가 |

### 상품 색상 (`tb_product_group_phone_color`)

**목적**: 상품별 색상 옵션

| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| `color_no` | int(11) | 색상 번호 (PK) |
| `product_group_code` | varchar(100) | 상품 그룹 코드 (FK) |
| `color_nm` | varchar(100) | 색상명 |

### 요금제 (`tb_rate_plan_phone`)

**목적**: 통신사 요금제 정보

| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| `rate_plan_code` | varchar(100) | 요금제 코드 (PK) |
| `carrier_code` | varchar(10) | 통신사 코드 |
| `rate_plan_nm` | varchar(255) | 요금제명 |
| `month_fee` | int(11) | 월 요금 |

---

## 판매점 관련 테이블

### 에이전시 계정 (`tb_agency_account`)

**목적**: 판매점 관리자 계정

| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| `agency_no` | int(11) | 에이전시 번호 (PK) |
| `agency_id` | varchar(100) | 에이전시 ID (로그인) |
| `agency_pw` | varchar(255) | 비밀번호 (해시) |
| `agency_nm` | varchar(100) | 담당자명 |

**데이터**: 56개

### 에이전시-판매점 매핑 (`tb_agency_store_mapp`)

**목적**: 에이전시 계정과 판매점 연결

| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| `agency_no` | int(11) | 에이전시 번호 (FK) |
| `store_no` | int(11) | 판매점 번호 (FK) |

---

## 코드 테이블

### 공통 코드 (`tb_common_code`)

**목적**: 시스템 전반에 사용되는 코드 관리

| 컬럼명 | 타입 | 설명 |
|--------|------|------|
| `code` | varchar(20) | 코드값 (PK) |
| `group_code` | varchar(20) | 그룹 코드 |
| `code_nm` | varchar(100) | 코드명 |
| `code_desc` | varchar(500) | 설명 |

**데이터**: 147개

#### 주요 그룹 코드

| 그룹 코드 | 설명 | 예시 |
|----------|------|------|
| 0201 | 신청 진행 상태 | 0201001~0201007 (신청접수~취소) |
| 0202 | 판매점 승인 상태 | 0202003 (승인완료) |
| 0204 | 상품 상태 | 0204002 (활성) |
| 0301001 | 통신사 | 0301001001 (SKT) |
| 0301007 | 가입 유형 | 0301007001 (신규) |

### 지역 코드

#### `tb_area_sido` (시도)
- **데이터**: 17개 (서울, 경기 등)

#### `tb_area_sigungu` (시군구)
- **데이터**: 219개

---

## 데이터 암호화

### 암호화 컬럼

노피는 개인정보 보호를 위해 AES 암호화 사용:

| 테이블 | 암호화 컬럼 | 복호화 방법 |
|--------|-----------|-----------|
| `tb_user` | `user_id`, `user_nm`, `user_tel_no` | `AES_DECRYPT(UNHEX(컬럼), secret_key)` |
| `tb_apply_*` | `user_nm`, `apply_tel_no`, `completed_tel_no` | 동일 |
| `tb_store` | `store_nm`, `business_no`, `tel_no` | 동일 |

**Secret Key**: 환경변수로 관리

---

## 인덱스 전략

주요 테이블의 인덱스:

### `tb_apply_phone`
- **PRIMARY**: `apply_no`
- **INDEX**: `user_no`, `store_no`, `step_code`, `created_at`

### `tb_apply_campaign_phone`
- **PRIMARY**: `apply_no`
- **INDEX**: `user_no`, `store_no`, `step_code`, `created_at`

### `tb_store_purchase`
- **PRIMARY**: `purchase_no`
- **INDEX**: `apply_no`, `store_no`, `created_at`

---

## 데이터 수집 방법

### 스키마 수집
```bash
python3 3-scripts/collectors/collect_db_schema.py
```

### 데이터 샘플 확인
```sql
-- 최근 신청 10건
SELECT * FROM tb_apply_campaign_phone
WHERE deleted_yn = 'N'
ORDER BY created_at DESC
LIMIT 10;

-- 개통 완료 건
SELECT * FROM tb_apply_campaign_phone
WHERE deleted_yn = 'N' AND step_code = '0201005'
ORDER BY completed_at DESC;
```

---

## 참고 자료

- [APPLICATION_FUNNEL_ANALYSIS.md](APPLICATION_FUNNEL_ANALYSIS.md) - 신청 퍼널 분석
- [SERVICE_VERSION_HISTORY.md](SERVICE_VERSION_HISTORY.md) - 서비스 버전 히스토리
- [DATA_DICTIONARY.md](DATA_DICTIONARY.md) - 데이터 사전

---

**작성자**: Data Team
**최종 업데이트**: 2025-11-19
**버전**: 2.0
